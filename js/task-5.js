function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}

const btn = document.getElementsByClassName("change-color");

btn.addEventListener("click", getRandomHexColor)